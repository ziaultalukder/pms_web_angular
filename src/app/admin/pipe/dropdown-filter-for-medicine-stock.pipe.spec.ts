import { DropdownFilterForMedicineStockPipe } from './dropdown-filter-for-medicine-stock.pipe';

describe('DropdownFilterForMedicineStockPipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownFilterForMedicineStockPipe();
    expect(pipe).toBeTruthy();
  });
});
